# HVAC Production Metro

Streamlit mock dashboard based on the revised July 2026 equipment inventory.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

Upload all files to one GitHub repository and select `app.py` in Streamlit Community Cloud.

The order list and processing-time assumptions are examples. Replace them with validated shop-floor data before operational use.
